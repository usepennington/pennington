@namespace DocSiteChromeOverridesExample.Components

@* Rendered into the <head> of every page when passed to
   DocSiteOptions.AdditionalHtmlHeadContent. This component is the
   component-authored counterpart to SiteChromeOverrides.BuildHtmlHeadContent
   — the how-to shows both the literal-string and component-rendered shapes
   side by side. *@

<meta name="x-chrome-overrides-head" content="extra-head-fragment">
<link rel="preconnect" href="https://example.com">
