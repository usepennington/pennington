---
title: API
description: Second content area demonstrating how DocSite groups pages under a separate URL prefix.
order: 10
uid: kitchen-sink.api.index
---

This is the landing page for the **API** area. It sits at `/api/` and
shares the same markdown pipeline as the **Main** area — the two areas
are separated by their top-level folder under `Content/` and by their
`ContentArea.Slug` in `DocSiteOptions.Areas`.

See [Reference example](/api/reference-example/) for a sample API page.
