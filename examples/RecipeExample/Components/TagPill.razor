<span class="@($"bg-primary-100 ring-1 ring-primary-500 text-primary-500 px-3 py-1 rounded-full text-xs {CssClass}")">
    @Text
</span>

@code {
    [Parameter, EditorRequired] public string Text { get; set; } = string.Empty;
    [Parameter] public string CssClass { get; set; } = string.Empty;
}
