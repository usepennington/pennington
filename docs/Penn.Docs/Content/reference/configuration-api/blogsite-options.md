---
title: "BlogSiteOptions"
description: "Complete reference for BlogSiteOptions: all properties and supporting records (SocialLink, HeaderLink, Project, HeroContent, BlogPostPage) including blog paths, author info, RSS/sitemap, social media image factory"
uid: "penn.reference.blogsite-options"
order: 30
---

Table-format reference for every `BlogSiteOptions` property. Include supporting record definitions: `SocialLink` (Icon RenderFragment, Url), `HeaderLink` (Title, Url), `Project` (Title, Description, Url), `HeroContent` (Title, Description), and `BlogPostPage`. Document what `AddBlogSite` registers and the `UseBlogSite` middleware order. Document the `BlogSiteFrontMatter` fields alongside `BlogSiteOptions` since they're tightly coupled — or cross-reference the front matter system page.
