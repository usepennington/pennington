---
title: "PennOptions"
description: "Complete reference for PennOptions: SiteTitle, SiteDescription, CanonicalBaseUrl, ContentRootPath, HighlightingOptions, IslandsOptions, LocalizationOptions, TranslationOptions, AddMarkdownContent<T>, AddLlmsTxt, and AdditionalRoutingAssemblies"
uid: "penn.reference.penn-options"
order: 10
---

Definitive reference for `PennOptions` — every property, nested options object, and method. Use a table format for scalar properties (name, type, default, description). For nested options objects (`HighlightingOptions`, `IslandsOptions`, `LocalizationOptions`, `TranslationOptions`), provide a sub-table or dedicated subsection with their own properties and methods. Document the `AddMarkdownContent<T>` fluent method and its `MarkdownContentOptions` parameter type (ContentPath, BasePageUrl, Section, SearchPriority). Document `AddLlmsTxt()` and `LlmsTxtOptions`. Document `AdditionalRoutingAssemblies`. Use `:xmldocid` code blocks to pull the actual type definitions from source where feasible. This page should be the single authoritative source — if a user wants to know "what can I set on PennOptions?", this page has the complete answer.
