| URL | Title | Reason | Fix |
|---|---|---|---|
| /tutorials/getting-started/deploy-github-pages | Ship it — build and deploy to GitHub Pages | No example project exposes an xmldocid-addressable symbol for `RunOrBuildAsync(args)` wiring or `BuildReport` inspection (all examples keep it in top-level Program.cs statements), no example ships a GitHub Actions deploy workflow, and Pennington core has no `.nojekyll` emission or handling (zero hits for "nojekyll" under src/). | Add a `BuildHost` helper method (e.g., `M:MinimalExample.BuildHost.RunOrBuildAsync`) that wraps `app.RunOrBuildAsync(args)` plus a demo `M:MinimalExample.BuildHost.PrintBuildReport(Pennington.Generation.BuildReport)` so the tutorial can fence small symbols; add a `.github/workflows/deploy.yml` raw-file fence candidate under `examples/MinimalExample/`; and confirm whether Pennington emits `.nojekyll` automatically or document the manual step explicitly. |
| /tutorials/getting-started/styling | Style the site with MonorailCSS | Every example project that wires `AddMonorailCss` / `UseMonorailCss` / `NamedColorScheme` (MinimalExample, RecipeExample, SpectreConsoleExample, etc.) does so via top-level `Program.cs` statements, so no xmldocid target exists for the MonorailCSS service registration, the `NamedColorScheme` configuration, or the `UseMonorailCss()` pipeline step that the tutorial must show — and `MainLayout.razor` utility classes are a Razor component, not a C# symbol addressable by xmldocid. | Add short named methods to `MinimalExample` (e.g., a `Startup` static class with `M:MinimalExample.Startup.AddStyling(Microsoft.Extensions.DependencyInjection.IServiceCollection)` returning an `IServiceCollection` after calling `AddMonorailCss` with a `NamedColorScheme`, and `M:MinimalExample.Startup.UseStyling(Microsoft.AspNetCore.Builder.WebApplication)` wrapping `UseMonorailCss`) so each step has a real xmldocid target; and list `examples/MinimalExample/Components/Layout/MainLayout.razor` as an explicit raw-file fence candidate in `examples-inventory.md` for the "add a utility class to a layout" step. |
| /tutorials/docsite/sections-and-areas | Organize content with sections and areas | No project under `examples/` uses `DocSiteOptions.Areas` or constructs a `ContentArea` — the only working demonstration of `Areas` lives in `docs/Pennington.Docs/Program.cs`, which is outside the examples inventory and therefore not a valid raw-file fence source. Sections can be grounded (BeaconDocsExample, NorthwindHandbookExample, SpectreConsoleExample all cover `section:` / `order:` / `md.Section = ...`), but the TOC row makes "areas" a headline topic alongside sections, so grounding only half the page would break the scope contract. | Add an example project that wires `AddDocSite(... Areas = [ new ContentArea("Docs", "docs"), new ContentArea("API", "api") ])` with two matching top-level `Content/` subfolders (e.g., `examples/DocSiteAreasExample/`), then add its `Program.cs` and one `Content/<slug>/index.md` per area to `examples-inventory.md` as raw-file fence candidates; or, alternatively, list `docs/Pennington.Docs/Program.cs` explicitly in `examples-inventory.md` as an allowed raw-file fence source for the Areas snippet. |
| /how-to/content-authoring/ui-components-in-markdown | Use UI components inside markdown | Pennington has no mechanism to render Razor component tags embedded in markdown source. `MarkdownContentRenderer` (`src/Pennington/Markdown/MarkdownContentRenderer.cs`) produces HTML via Markdig, then the result is injected into Razor via `@((MarkupString)HtmlContent)` in `src/Pennington.DocSite/Slots/Components/DocSiteArticle.razor` and `src/Pennington.BlogSite/Components/Layout/BlogPost.razor` — `MarkupString` emits literal HTML and Blazor never re-parses it as components. There is no Markdig extension, `ICodeBlockPreprocessor`, or `IHtmlResponseRewriter` in `src/Pennington/Markdown/` or `src/Pennington/Infrastructure/` that resolves custom element names like `<Steps>`, `<Card>`, `<CardGrid>`, `<LinkCard>`, or `<Badge>`; the only HTML rewriters registered are `XrefHtmlRewriter`, `LocaleLinkHtmlRewriter`, and `BaseUrlHtmlRewriter`, none of which resolve component tags. Confirmed by `examples/BeaconDocsExample/Content/index.md` (which authors `<LinkCard ... />`) producing `examples/BeaconDocsExample/output/index.html` lines 42–44 where `<LinkCard>` survives verbatim as an unrecognized custom element. Zero hits for "Steps", "LinkCard", "CardGrid", "Badge", or "Card" as tag names or component registrations anywhere under `src/Pennington/`. The `Pennington.UI` components (`src/Pennington.UI/Components/Steps.razor`, `Card.razor`, `CardGrid.razor`, `LinkCard.razor`, `Badge.razor`) are Razor primitives intended for layout/page composition only. | Either (a) add a Markdig inline/block extension plus DI hook (e.g., `penn.AddMarkdownComponent<LinkCard>("LinkCard")`) that matches registered component tag names during rendering and dispatches to `ComponentRenderer.RenderComponentAsync<T>` with attribute-derived parameters, then ship an `examples/MarkdownComponentsExample/` project exercising each of `<Steps>`/`<Step>`, `<Card>`, `<CardGrid>`, `<LinkCard>`, and `<Badge>` from a markdown file with xmldocid-addressable registration symbols; or (b) drop this TOC row entirely, since no subset of the listed components currently works in markdown and the "Does not cover" line already excludes authoring custom components. |
| /tutorials/beyond-basics/custom-razor-component | Author a custom Razor component for your content | Pennington has no mechanism to register a Razor component so it can be invoked from inside markdown via element attributes — the markdown pipeline renders raw `<Component />` tags through `MarkupString` as literal unrecognized HTML (verified against `examples/BeaconDocsExample/output/index.html`, where `<LinkCard ... />` survives verbatim and is never rendered by Blazor). `MarkdownContentRenderer` has no component-dispatch extension, no `ICodeBlockPreprocessor` or AngleSharp rewriter resolves custom tag names, and no example project registers a user-authored component that surfaces inside article HTML. The closest working pattern is the island renderer (`T:Pennington.Islands.RazorIslandRenderer\`1`), but islands are page-level slots wired by `IIslandRenderer.IslandName`, not markdown-embedded components with attribute-passed parameters — which is a different topic (already excluded by the TOC "Does not cover" line). | Either (a) add a Markdig extension + DI registration to Pennington core that resolves user-registered Razor component tags inside markdown and renders them through `ComponentRenderer.RenderComponentAsync<T>`, then ship an example project (e.g., `examples/CustomComponentExample/Components/Callout.razor` plus a `services.AddPennington(... penn => penn.RegisterMarkdownComponent<Callout>("callout"))` call) with xmldocid-addressable registration symbols; or (b) retitle the TOC row to "Use a custom Razor component in your layout" so the tutorial can ground on `examples/UserInterfaceExample/Components/Layout/Pages.razor` consuming `OutlineNavigation` as a layout-level component, which is the only Razor-component authoring pattern actually supported today. |
| /how-to/extensibility/code-block-preprocessor | Add a custom code-block preprocessor | No example project under `examples/` authors an `ICodeBlockPreprocessor` implementation. The only implementation in the repo is `Pennington.Roslyn.Preprocessing.RoslynCodeBlockPreprocessor` (`src/Pennington.Roslyn/Preprocessing/RoslynCodeBlockPreprocessor.cs`), which ships inside the `Pennington.Roslyn` NuGet package and is registered by `AddPenningtonRoslyn` — not something a user implements in their own site. The scope of this page calls for showing a user implementing the interface, setting `Priority`, and returning a `CodeBlockPreprocessResult` with `SkipTransform` — without any xmldocid-addressable user-authored preprocessor in `examples/`, every step would have to be fabricated as inline C#, which violates the "xmldocid examples referencing examples/ projects" convention. The `RoslynIntegrationExample` project was suggested as a model but only consumes `AddPenningtonRoslyn`; it does not implement `ICodeBlockPreprocessor` itself. Verified: zero hits for `ICodeBlockPreprocessor`, `CodeBlockPreprocessResult`, or a class name ending in `Preprocessor` anywhere under `examples/`. `ForgePortalExample` authors a custom `ICodeHighlighter` (`PipelineHighlighter`) but that is a different interface and belongs to the next page. | Add a small example project (e.g., `examples/CodeBlockPreprocessorExample/`) whose `Program.cs` wires `AddPennington` plus `services.AddSingleton<ICodeBlockPreprocessor, MermaidCodeBlockPreprocessor>()` (or similar), with a preprocessor class exposing `P:...MermaidCodeBlockPreprocessor.Priority` and `M:...MermaidCodeBlockPreprocessor.TryProcess(System.String,System.String)` as xmldocid-addressable symbols returning `new CodeBlockPreprocessResult(HighlightedHtml, baseLanguage, SkipTransform: true)` for a recognizable fence language (e.g., `mermaid` or `dot`). Once that example exists, add it to `examples-inventory.md` and then this how-to can be grounded on it. |
| /reference/ui/content | Content components | The TOC row requires "sample Razor markup per component" for `Card`, `CardGrid`, `LinkCard`, `Badge`, `Step`, `Steps`, and `CodeBlock` (all verified in `src/Pennington.UI/Components/`), and the Pennington docs convention (per user memory `feedback_docs_conventions.md` and the `reference/_template.md` Example section) mandates that code samples be xmldocid fences pointing at symbols in `examples/` projects. No project under `examples/` contains Razor call sites for any of these components as page/layout markup — `grep` across `examples/` for `<Card `, `<CardGrid `, `<LinkCard `, `<Badge `, `<Step `, `<Steps `, `<CodeBlock ` returns zero Razor usages (the only hit is `examples/BeaconDocsExample/Content/index.md`, which authors `<LinkCard />` inside markdown — an unsupported path already recorded as the `/how-to/content-authoring/ui-components-in-markdown` blocker above, where the tag survives verbatim in generated HTML and is never rendered). `UserInterfaceExample` imports `@using Pennington.UI.Components` in its `_Imports.razor` but no `.razor` file in the project actually instantiates any of the seven components. Per-`[Parameter]` enumeration is verified (`Card`: `ChildContent`, `Icon`, `Title`, `Color` default `"primary"`; `CardGrid`: `Columns` default `"2"`, `ChildContent`; `LinkCard`: `ChildContent`, `Icon`, `Title`, `Href`, `Color` default `"primary"`; `Badge`: `Text`, `Variant` default `"note"` with accepted values `success`/`tip`/`caution`/`danger`/`note`, `Size` default `"medium"` with accepted values `small`/`medium`/`large`; `Step`: `StepNumber` default `"1"`, `ChildContent`; `Steps`: `Type` default `"primary"`, `ChildContent`; `CodeBlock`: `Language` `[EditorRequired]`, `Code`, `ChildContent`, `IsInTabGroup` default `false`), but there is no xmldocid-addressable Razor fragment in `examples/` that can back the "minimal Razor example" per component. Writing the page would force inline-literal Razor snippets, which violates the xmldocid-only rule. | Add an `examples/UiComponentsExample/` (or extend `UserInterfaceExample`) with one small `.razor` page per component (e.g., `CardExample.razor`, `CardGridExample.razor`, `LinkCardExample.razor`, `BadgeExample.razor`, `StepsExample.razor`, `CodeBlockExample.razor`) that instantiates the component with realistic parameter values, then add each page to `examples-inventory.md` as a raw-file fence candidate. Razor files can be fenced as raw-file snippets (since they are not C# symbols with xmldocid); alternatively, wrap each usage in a named method (e.g., `M:UiComponentsExample.Samples.BuildCard`) that returns a `RenderFragment`, giving proper xmldocid targets. Once an example ships, this reference page can be grounded. |
| /how-to/content-authoring/cross-references | Cross-reference pages by `uid` | The TOC row names `<xref uid="…">text</xref>` as a supported syntax, but that element form does not exist in Pennington. `XrefResolvingService.XrefTagRegex()` is `<xref:([^>]+)>` (verified in `src/Pennington/Infrastructure/XrefResolvingService.cs` line 138) — a self-closing `<xref:uid>` tag whose anchor text is auto-filled from the resolved page title, with no mechanism to pass custom display text via an attribute on that form. The two syntaxes that actually work are (1) the self-closing raw tag `<xref:uid>` (Phase 1 regex substitution, no custom text) and (2) the Markdown link `[text](xref:uid)` which becomes `<a href="xref:uid">text</a>` and is rewritten in the DOM phase (Phase 2, preserves custom text). Writing the page against the TOC row as stated would fabricate an element form that ships nowhere in `src/` or `examples/` (verified: zero hits for `<xref uid=` or `</xref>` across the repo, and BeaconDocsExample's markdown files only use `[text](xref:uid)`). | Retitle/rescope the TOC row to name the two real syntaxes: "linking via `<xref:uid>` (self-closing, auto-titled) or `[text](xref:uid)` (Markdown link with custom text)". Once the scope contract matches `XrefResolvingService`, the page can be grounded on `examples/BeaconDocsExample/Content/guides/configuration.md` (front-matter `uid: "beacon.configuration"` plus a `[text](xref:beacon.api-reference)` link) and `examples/BeaconDocsExample/Content/api/index.md` (the target page declaring `uid: "beacon.api-reference"`). Alternatively, add a Markdig inline-parser plus an `IHtmlResponseRewriter` that recognises `<xref uid="…">text</xref>` as a real element form, and ship an example project exercising it, before the page can be written as currently scoped. |
